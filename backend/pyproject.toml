[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "langchain-openai>=1.1.10",
    "langgraph>=1.0.10",
    "langsmith>=0.7.14",
    "mcp[cli]>=1.26.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
]
