services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: arc_agent
    ports:
      - "5432:5432"
    volumes:
      - arc-agent-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d arc_agent"]
      interval: 5s
      timeout: 5s
      retries: 10

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:2024}
      NEXT_PUBLIC_ASSISTANT_ID: ${NEXT_PUBLIC_ASSISTANT_ID:-agent}
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
      NEXT_DISABLE_ESLINT: "1"
    ports:
      - "3000:3000"
    volumes:
      - ../frontend:/app
      - frontend-node_modules:/app/node_modules
      - frontend-app-web-node_modules:/app/apps/web/node_modules
      - frontend-app-web-next:/app/apps/web/.next
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  arc-agent-pgdata:
  frontend-node_modules:
  frontend-app-web-node_modules:
  frontend-app-web-next:
